.gradle/
build/
app/build/

# Local Android / IDE state
/local.properties
.idea/
.vscode/
*.iml
captures/
.DS_Store
.cursorindexingignore
.specstory/

# Signing material must never be committed. Use your own local keystore.
/keystore/
*.keystore
*.jks
*.p12
*.pem
*.key

# Build outputs are distributed through GitHub Releases, not tracked in git.
/dist/
*.apk
*.aab

# Local publishing drafts
docs/WECHAT*.md
docs/assets/wechat-cover*.png

# Local-only handoff / Codex relay notes (NEVER published to GitHub)
/local/

# iOS — Xcode 工程由 ios/project.yml 通过 `xcodegen generate` 生成
ios/MDReader.xcodeproj/
ios/build/
ios/DerivedData/
**/xcuserdata/
*.xcarchive
