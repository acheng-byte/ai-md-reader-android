# MD阅读器 v2.2.12

## 改进

- **日志系统优化**：倒序显示（最新在前），默认只显示警告和错误，支持切换查看全部；标题显示错误数量
- **大幅减少日志噪音**：成功操作不再打日志，只记录异常和失败；启动时不再重复打印 Vault 状态
- **防止重复扫描**：增加扫描锁，多线程并发请求不会重复扫描库文件夹
- **新增「清空」按钮**：Vault 设置中可一键清空库文件夹，方便重新选择
