## v2.3.2 更新日志

### 修复

- **阅读统计文案**：将"ReadAny"改为"MD阅读器"，显示更准确的应用名称
- **导出 DOC 闪退**：改用纯文本方式导出，避免 HTML 解析导致的崩溃；增强异常捕获，任何错误都会友好提示而非闪退
- **XML 实体转义**：修复 `preprocessHtmlForXml` 中裸露 `&` 字符未转义的问题，防止 XML 解析崩溃
